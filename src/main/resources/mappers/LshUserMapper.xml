<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.human.dalligo.dao.LshUserDAO">

    <select id="selectUserByUserId" parameterType="string"
        resultType="com.human.dalligo.vo.LshUserVO">
        SELECT id, user_id, password, name, nick_name, address, phone, is_admin
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="selectUserById" parameterType="int"
        resultType="com.human.dalligo.vo.LshUserVO">
        SELECT id, user_id, password, name, nick_name, address, phone, is_admin
        FROM user
        WHERE id = #{id}
    </select>

</mapper>
